# SPI-Protocol

SPI (Serial Peripheral Interface) is a synchronous serial communication protocol used for high-speed data transfer between a master device and one or more slave devices.

## contents
* SPI_Master --> Sends Data to The Slave.
* TEST --> Test Bench acts as Slave and Responds to the Master.

## Languages Used
* Verilog
* System Verilog

## Tools Used
EDA Playground

## about SPI

* SPI (Serial Peripheral Interface) is a synchronous serial communication protocol used for high-speed data transfer between a master device and one or more slave devices.

* SPI communication uses four main signals:

-> MOSI (Master Out Slave In) → Data sent from master to slave
-> MISO (Master In Slave Out) → Data sent from slave to master
-> SCLK (Serial Clock) → Clock generated by master
-> SS/CS (Slave Select/Chip Select) → Selects the slave device

* Unlike UART, SPI is a synchronous protocol because communication happens with respect to the clock signal generated by the master.

* SPI supports full-duplex communication, meaning data can be transmitted and received simultaneously. Due to its high speed and simple architecture.

## Waveform
<img width="1831" height="435" alt="Screenshot 2026-05-19 154641" src="https://github.com/user-attachments/assets/c75f414f-2e1b-4064-b4e1-7f2b114d2629" />
